import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ProfilePass = () => {
  return (
    <View>
      <Text>ProfilePass</Text>
    </View>
  );
};

export default ProfilePass;

const styles = StyleSheet.create({});
